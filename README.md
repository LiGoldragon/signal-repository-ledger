# signal-repository-ledger

Ordinary Signal contract for the repository ledger component.

The first slice covers Gitolite receive-hook notifications and basic read
queries. Runtime storage lives in `repository-ledger`; meta-signal policy lives
in `meta-signal-repository-ledger`.
